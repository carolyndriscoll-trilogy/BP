diff --git a/node_modules/@assistant-ui/react/dist/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.js b/node_modules/@assistant-ui/react/dist/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.js
index e83f52f..6d8ca5e 100644
--- a/node_modules/@assistant-ui/react/dist/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.js
+++ b/node_modules/@assistant-ui/react/dist/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.js
@@ -131,7 +131,22 @@ export function useToolInvocations({ state, getTools, onResult, setToolStatuses,
                                             };
                                             return; // Continue to next content part
                                         }
-                                        throw new Error(`Tool call argsText can only be appended, not updated: ${content.argsText} does not start with ${lastState.argsText}`);
+                                        // PATCH: Instead of crashing when argsText can't be appended,
+                                        // accept the new value. This handles multi-step streaming where
+                                        // the SDK re-serializes or restarts tool call args.
+                                        {
+                                            const newComplete = isArgsTextComplete(content.argsText);
+                                            if (newComplete) {
+                                                lastState.controller.argsText.close();
+                                            }
+                                            lastToolStates.current[content.toolCallId] = {
+                                                argsText: content.argsText,
+                                                hasResult: lastState.hasResult,
+                                                argsComplete: newComplete,
+                                                controller: lastState.controller,
+                                            };
+                                            return;
+                                        }
                                     }
                                     const argsTextDelta = content.argsText.slice(lastState.argsText.length);
                                     lastState.controller.argsText.append(argsTextDelta);
diff --git a/node_modules/@assistant-ui/react/src/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.ts b/node_modules/@assistant-ui/react/src/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.ts
index 4c553b1..0ee9bf9 100644
--- a/node_modules/@assistant-ui/react/src/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.ts
+++ b/node_modules/@assistant-ui/react/src/legacy-runtime/runtime-cores/assistant-transport/useToolInvocations.ts
@@ -208,9 +208,22 @@ export function useToolInvocations({
                       };
                       return; // Continue to next content part
                     }
-                    throw new Error(
-                      `Tool call argsText can only be appended, not updated: ${content.argsText} does not start with ${lastState.argsText}`,
-                    );
+                    // PATCH: Instead of crashing when argsText can't be appended,
+                    // accept the new value. This handles multi-step streaming where
+                    // the SDK re-serializes or restarts tool call args.
+                    {
+                      const newComplete = isArgsTextComplete(content.argsText);
+                      if (newComplete) {
+                        lastState.controller.argsText.close();
+                      }
+                      lastToolStates.current[content.toolCallId] = {
+                        argsText: content.argsText,
+                        hasResult: lastState.hasResult,
+                        argsComplete: newComplete,
+                        controller: lastState.controller,
+                      };
+                      return;
+                    }
                   }
 
                   const argsTextDelta = content.argsText.slice(
